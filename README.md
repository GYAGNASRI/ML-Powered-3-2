
  # ML-Powered Student Platform

  This is a code bundle for ML-Powered Student Platform. The original project is available at https://www.figma.com/design/Ly72OIM7ca825FkmHoys0y/ML-Powered-Student-Platform.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  